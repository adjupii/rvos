import styled from 'styled-components';

export default styled.div`
  position: absolute;
  left: 0;
  width: 74px;
  text-align: center;

  .toggle-nav {
    &__icon {
      cursor: pointer;
    }
  }
`;