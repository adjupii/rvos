import styled from 'styled-components';

export default styled.div`
  &.create-claim-policy {
    text-align: right;
    margin: -20px 0 15px;
    max-width: 471px;
  }

  .slider-arrows {
    &__icon {
      cursor: pointer;

      &--next {
        margin-left: 10px;
      }
    }
  }
`;